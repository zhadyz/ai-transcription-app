# ====================  REAL-TIME TRANSCRIPTION DEPENDENCIES  ====================
# Install with: pip install -r requirements-realtime.txt

# Audio processing for streaming
# Note: librosa and soundfile are optional - we can use numpy which is already installed
# Uncomment if you need advanced audio processing:
# librosa==0.10.2
# soundfile==0.12.1

# Core dependencies (already in main requirements.txt):
# - faster-whisper==1.0.3 ✅ (already installed)
# - numpy ✅ (already installed)
# - websockets==13.1 ✅ (already installed)

# Optional: Better Voice Activity Detection (VAD)
# silero-vad==5.1  # Uncomment for production-grade VAD

# ✅ ALL REQUIRED DEPENDENCIES ALREADY INSTALLED!
# The main requirements.txt already has everything we need.
